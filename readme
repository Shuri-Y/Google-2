## 課題1
サンプルコードをもとにハッシュテーブルを用いたデータ構造の辞書の、要素の削除を実装した。
<br>
ハッシュ値が重複していることがあるため、データはリストでつながれたものとして保存してあり、リストの次のデータが何であるかの情報を持っている。
よって削除するためには、削除対象のデータのひとつ前のデータとひとつ後のデータをつなげばよい。
<br>

**例**
a→b→c→d　のリストからbを削除する。<br>
ここで、aは次がbであるという情報を持っている。
これをaは次が **c** であるという情報、に書き換える。


## 課題2
ハッシュテーブルを用いたデータベースが実際のデータベースで使われていることが少ないことに関しての考察。


## 課題3
課題1で考案したような構造を持つホームページのリンクをまとめたデータからキャッシュの保持、削除を行うものである。制作仮定であるが提出する。
<br>
課題1とハッシュ値の計算の仕方の部分が大きく違っている。ハッシュ値は大きくなってしまうが、

**文字コード　×　入力された文字の場所　×　256**

を足し合わせることで文字列をアナグラムであったとしても固有の値にできる。ここでの入力された文字の場所とは、文字列の末尾を０、その隣を１…といったように指定する。<br>
例　"abc" = 97*256^2 + 98*256^1 +99